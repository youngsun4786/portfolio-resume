---
import { Button } from "@/components/ui/button"
import ThemeProvider from "@/components/ThemeProvider.astro";
// import { ModeToggle } from "./ModeToggle"
---

<header class="my-4 sticky top-0 z-50 w-full bg-background/100">
  <div class=" mx-auto px-2 w-full flex h-16 items-center space-x-4 sm:justify-between sm:space-x-0 lg:flex-row">
    <a href="/" class="text-2xl font-light">Youngsun Yoo</a>
    <nav class="flex items-center space-x-1">
      <Button variant="ghost" size="sm" asChild>
        <a href="/about">About</a>
      </Button>
      <Button variant="ghost" size="sm" asChild>
        <a href="/work">Work</a>
      </Button>
      <Button variant="ghost" size="sm" asChild>
        <a href="/contact">Contact</a>
      </Button>
      <ThemeProvider/>
      <!-- <ModeToggle client:load /> -->
    </nav>
  </div>
</header>
